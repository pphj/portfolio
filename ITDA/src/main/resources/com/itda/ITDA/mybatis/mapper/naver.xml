<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itda.ITDA.mybatis.mapper.NaverMapper">

    <insert id="saveNaverUserInfo" parameterType="com.itda.ITDA.domain.NaverDTO">
        insert into naver (
        name, email, nickname, gender, age, phone)
        values (#{name}, #{email}, #{nickname}, #{gender}, #{age}, #{phone})
    </insert>

</mapper>
